# Generated checkpoints

This directory records stable output paths and contains no weights in Git.

```text
checkpoints/
├── diffusion/<dataset>/{last.ckpt,best_fid.ckpt}
├── nam/<dataset>/nam_latest.pt
├── downstream/<dataset>/<model>/{best.pt,latest.pt}
└── */<run>/...                 # timestamped experiment history
```
