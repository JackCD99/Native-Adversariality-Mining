# MAISI experiment checkpoints

Task ControlNet fine-tuning writes `diffusion/last.pt` and lowest-FID `diffusion/best.pt`. NAM writes the stable 3D miner to `nam/nam_latest.pt`. Generated checkpoints are intentionally excluded from Git.
