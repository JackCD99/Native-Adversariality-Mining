# Official MAISI weights

Use the official `maisi3d-ddpm` model-zoo download path documented in `../README.md`. The default configuration expects `autoencoder_v1.pt` and `diff_unet_3d_ddpm.pt` in this directory. Weight files are governed by NSCLv1 and must not be committed to this repository.
