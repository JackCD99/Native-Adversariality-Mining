# VolDiT experiment checkpoints

Task fine-tuning writes `diffusion/last.pth` and the lowest-FID `diffusion/best.pth`. NAM training writes the stable miner to `nam/nam_latest.pt`. These generated binaries are intentionally excluded from source control.
